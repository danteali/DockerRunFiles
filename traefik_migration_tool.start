#!/bin/bash

NAME="traefik_migration_tool"

docker rm -f -v $NAME

#docker run --rm \
#  -v /storage/Docker/traefik_v2/migration_tool_old:/data_old \
#  -v /storage/Docker/traefik_v2/migration_tool_new:/data_new \
#  containous/traefik-migration-tool \
#  acme -i /data_old/acme.json -o /data_new/acme.json
#
#docker run --rm \
#  -v /storage/Docker/traefik_v2/migration_tool_old:/data_old \
#  -v /storage/Docker/traefik_v2/migration_tool_new:/data_new \
#  containous/traefik-migration-tool \
#  static -i /data_old/traefik.toml -d /data_new

docker run --rm \
  -v /storage/Docker/traefik_v2/migration_tool_old/merged_rules:/data_old \
  -v /storage/Docker/traefik_v2/migration_tool_new/merged_rules:/data_new \
  containous/traefik-migration-tool \
  static -i /data_old/traefik_merged.toml -d /data_new